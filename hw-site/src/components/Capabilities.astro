---
import { capabilities } from "../data/content";
import Icon from "./Icon.astro";
import type { IconName } from "../icons/icons";

// Icon je Fähigkeit (Schlüssel = Titel in content.ts)
const capIcon: Record<string, IconName> = {
  "Abläufe analysieren": "cap-process-analysis",
  "AI-Chancen bewerten": "cap-ai-strategy",
  "Abläufe neu gestalten": "cap-process-redesign",
  "AI-Anwendungen bauen": "cap-ai-engineering",
  "Software bauen": "cap-software-engineering",
  "Modelle auswählen": "cap-model-engineering",
  "Cloud und Infrastruktur": "cap-cloud-infra",
  "Sicherheit": "cap-cybersecurity",
  "Regeln und Nachvollziehbarkeit": "cap-ai-governance",
  "Team befähigen": "cap-ai-enablement",
};
---

<ul class="caps">
  {capabilities.map((c) => (
    <li class="cap">
      {capIcon[c.title] && (
        <span class="cap__icon"><Icon name={capIcon[c.title]} size={24} /></span>
      )}
      <h3>{c.title}</h3>
      <p>{c.text}</p>
    </li>
  ))}
</ul>

<style>
  .caps {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 14px;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  }
  .cap {
    padding: 22px 22px 24px;
    border-radius: var(--radius-lg);
    background: var(--paper);
    border: 1px solid var(--line);
    display: grid;
    gap: 8px;
    align-content: start;
    transition: border-color 0.2s, transform 0.25s var(--ease-out);
  }
  .cap:hover { border-color: var(--line-strong); transform: translateY(-2px); }
  .cap__icon {
    display: inline-flex;
    color: var(--accent, var(--process));
    margin-bottom: 6px;
  }
  .cap h3 { font-size: 1.02rem; }
  .cap p { color: var(--fg-muted); font-size: 0.93rem; }
</style>
